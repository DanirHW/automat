plugins {
    id 'java'
}

group = 'ru.netology'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.testng:testng:7.1.0'
//проводим тест с testng   testImplementation platform('org.junit:junit-bom:5.9.1')
//проводим тест с testng   testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useTestNG()
    //проводим тест с testng  useJUnitPlatform()
}